from pathlib import Path
# ======================================================================================================================
# مسیر پایه پروژه
# BASE_DIR مسیر ریشه پروژه را مشخص می‌کند و برای مسیر دهی به فایل‌ها استفاده می‌شود
# ======================================================================================================================
BASE_DIR = Path(__file__).resolve().parent.parent

# ======================================================================================================================
# تنظیمات امنیتی و توسعه
# ======================================================================================================================
SECRET_KEY = 'django-insecure-s5(mw%45u6kw5v5amxq*uqd54q4jsmih2nhiv_r&o8q&-5@hu4'  # کلید امنیتی پروژه
DEBUG = True  # حالت توسعه (برای محیط تولید باید False شود)
ALLOWED_HOSTS = []  # دامنه‌های مجاز برای پروژه

# ======================================================================================================================
# برنامه‌های نصب شده
# این لیست شامل اپ‌ها و پلاگین‌هایی است که Django از آن‌ها استفاده می‌کند
# ======================================================================================================================
INSTALLED_APPS = [
    # اپ‌های داخلی Django
    'django.contrib.admin',        # پنل ادمین
    'django.contrib.auth',         # سیستم احراز هویت
    'django.contrib.contenttypes', # مدیریت انواع مدل‌ها
    'django.contrib.sessions',     # مدیریت سشن‌ها
    'django.contrib.messages',     # پیام‌های flash
    'django.contrib.staticfiles',  # مدیریت فایل‌های استاتیک

    # اپ‌های شخص ثالث
    'rest_framework',  # برای ایجاد API
    'drf_yasg',        # تولید مستندات OpenAPI / Swagger

    # اپ‌های خود پروژه
    'sysmonitor',      # اپ مانیتورینگ سیستم
]

# ======================================================================================================================
# Middleware ها
# Middlewareها پردازش‌هایی هستند که بین درخواست (Request) و پاسخ (Response) اعمال می‌شوند
# ======================================================================================================================
MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

# ======================================================================================================================
# تنظیمات URL اصلی پروژه
# ======================================================================================================================
ROOT_URLCONF = 'core.urls'

# ======================================================================================================================
# تنظیمات قالب‌ها (Templates)
# ======================================================================================================================
TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',  # موتور رندر قالب‌ها
        'DIRS': [BASE_DIR / 'templates'],  # مسیر پوشه قالب‌های پروژه
        'APP_DIRS': True,  # جستجوی قالب‌ها در هر اپ
        'OPTIONS': {
            'context_processors': [  # توابعی که متغیرهای عمومی را به قالب‌ها اضافه می‌کنند
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

# ======================================================================================================================
# تنظیمات WSGI
# ======================================================================================================================
WSGI_APPLICATION = 'core.wsgi.application'

# ======================================================================================================================
# پایگاه داده
# اینجا از SQLite برای توسعه استفاده شده است
# برای محیط تولید معمولاً PostgreSQL یا MySQL استفاده می‌شود
# ======================================================================================================================
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.sqlite3",  # موتور پایگاه داده
        "NAME": BASE_DIR / "db.sqlite3",        # مسیر فایل دیتابیس
    }
}

# ======================================================================================================================
# اعتبارسنجی رمز عبور
# ======================================================================================================================
AUTH_PASSWORD_VALIDATORS = [
    {'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator'},
    {'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator'},
    {'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator'},
    {'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator'},
]

# ======================================================================================================================
# بین‌المللی‌سازی و تنظیمات زمانی
# ======================================================================================================================
LANGUAGE_CODE = 'en-us'  # زبان پیش‌فرض
TIME_ZONE = 'UTC'        # منطقه زمانی
USE_I18N = True          # فعال کردن پشتیبانی از ترجمه
USE_TZ = True            # فعال کردن استفاده از زمان UTC

# ======================================================================================================================
# فایل‌های استاتیک و رسانه‌ای
# ======================================================================================================================
STATIC_URL = "/static/"                  # URL دسترسی به فایل‌های استاتیک
STATIC_ROOT = BASE_DIR / "staticfiles"  # مسیر خروجی جمع‌آوری استاتیک‌ها
MEDIA_URL = "/media/"                    # URL دسترسی به فایل‌های آپلود شده
MEDIA_ROOT = BASE_DIR / "media"         # مسیر ذخیره فایل‌های آپلود شده
STATICFILES_DIRS = [
    BASE_DIR / "static",                # مسیر پوشه فایل‌های استاتیک پروژه
]

# ======================================================================================================================
# نوع فیلد کلید اصلی پیش‌فرض
# ======================================================================================================================
DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'
# ======================================================================================================================